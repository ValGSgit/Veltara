Place sandbox GLB models here.

Current model keys expected by `apps/web/src/planet/models.js`:

- `beacon_tower` -> `beacon-tower.glb`
- `storage_crate` -> `storage-crate.glb`
- `crafting_forge` -> `crafting-forge.glb`
- `orb_core` -> `orb-core.glb`

If a model file is missing or fails to load, sandbox rendering falls back to built-in primitive geometry.
